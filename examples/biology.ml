% biology.ml — таксономія та успадкування властивостей
% Демонструє: два структурно однакових рекурсії, заперечення, глибоке дерево доведення

% Таксономія — є(вид, надвид)
є(горобець, птах).
є(орел, птах).
є(пінгвін, птах).
є(птах, тварина).
є(кіт, ссавець).
є(ссавець, тварина).
є(тварина, живе).
є(дуб, дерево).
є(дерево, рослина).
є(рослина, живе).
є(троянда, квітка).
є(квітка, рослина).
є(лосось, риба).
є(риба, тварина).

% Властивості
має(птах, крила).
має(птах, дзьоб).
має(ссавець, хутро).
має(риба, плавці).
має(рослина, хлорофіл).
має(живе, клітини).

% Виключення
не_літає(пінгвін).

% Правила — успадкування
правило успадковує(?x, ?p) якщо має(?x, ?p).
правило успадковує(?x, ?p) якщо є(?x, ?y) і успадковує(?y, ?p).

% Правило — літає якщо птах і не виключено
правило літає(?x) якщо є(?x, птах) і не не_літає(?x).

% Запити
?- літає(?x).
?- успадковує(дуб, ?p).
?- слід успадковує(дуб, хлорофіл).
