% mythology.ml — слов'янські боги та вірування
% Демонструє: моделювання культурних/релігійних структур

% Боги та їх домени
бог(перун).
бог(велес).
бог(дажбог).
бог(стрибог).
бог(мокоша).
бог(сварог).
бог(лада).

домен(перун, війна).
домен(перун, грім).
домен(велес, підземний_світ).
домен(велес, худоба).
домен(велес, магія).
домен(дажбог, сонце).
домен(стрибог, вітер).
домен(мокоша, родючість).
домен(мокоша, доля).
домен(сварог, вогонь).
домен(сварог, ковальство).
домен(лада, кохання).
домен(лада, гармонія).

% Вшанування — хто якого бога шанує
шанує(воїн, перун).
шанує(пастух, велес).
шанує(жрець, велес).
шанує(селянин, мокоша).
шанує(селянин, дажбог).
шанує(коваль, сварог).

% Суперечності між богами
ворожнеча(перун, велес).
ворожнеча(велес, перун).

% Правила
правило покровитель(?х, ?д) якщо шанує(?х, ?б) і домен(?б, ?д).
правило конфлікт_вірувань(?x, ?y) якщо шанує(?x, ?б1) і шанує(?y, ?б2) і ворожнеча(?б1, ?б2).

% Запити
?- покровитель(воїн, ?д).
?- конфлікт_вірувань(?x, ?y).
