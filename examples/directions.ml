% directions.ml — географія українських міст
% Демонструє: симетрію через два правила, транзитивні відношення

% Факти — місто на північ від іншого
на_північ(київ, черкаси).
на_північ(черкаси, кропивницький).
на_північ(харків, полтава).
на_північ(полтава, кременчук).
на_північ(львів, тернопіль).
на_північ(тернопіль, хмельницький).

% Факти — сусідні міста (прямий зв'язок)
сусід(київ, житомир).
сусід(київ, чернігів).
сусід(львів, ужгород).
сусід(харків, дніпро).
сусід(одеса, миколаїв).

% Правила — симетрія
правило поруч(?x, ?y) якщо сусід(?x, ?y).
правило поруч(?x, ?y) якщо сусід(?y, ?x).

% Правило — транзитивна північність
правило північніше(?x, ?y) якщо на_північ(?x, ?y).
правило північніше(?x, ?z) якщо на_північ(?x, ?y) і північніше(?y, ?z).

% Запити
?- поруч(житомир, київ).
?- поруч(київ, ?x).
?- північніше(київ, ?x).
